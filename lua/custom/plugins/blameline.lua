return {
  -- 'tveskag/nvim-blame-line',
  -- event = 'BufReadPost', -- Lazy-load on file open
  -- config = function()
  --   vim.g.blame_line_enabled = 1 -- enable by default (optional)
  --   vim.g.blame_line_delay = 1000 -- delay before showing blame (in ms)
  --   vim.g.blame_line_virtual_text = 0 -- set to 1 to show as virtual text
  --
  --   -- Optional: Keymap to toggle
  --   vim.keymap.set('n', '<leader>bl', function()
  --     vim.g.blame_line_enabled = 1 - vim.g.blame_line_enabled
  --     print('Blame line ' .. (vim.g.blame_line_enabled == 1 and 'enabled' or 'disabled'))
  --   end, { desc = 'Toggle Git Blame Line' })
  -- end,
}
